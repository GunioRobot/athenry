# Do not edit by hand, this file changes during runtime.
# Make changes to config.rb
#

TIMEZONE="<%= CONFIG.timezone %>"
SETS=( <%= CONFIG.sets %> )
PKG_MANAGER="<%= CONFIG.pkgmanager %>"
ARCH="<%= CONFIG.arch %>"

# Overlays Hash
<% CONFIG.overlay_array.each do |name| %>
<%= "#{name}" %>
<% end %>

# Temporary options
# These change depending on what your building do not edit!
FRESHEN="<%= CONFIG.freshen %>"
NOPALUDIS="<%= CONFIG.nopaludis %>"
