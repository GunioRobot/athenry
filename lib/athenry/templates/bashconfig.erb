TIMEZONE="<%= CONFIG.timezone %>"
SETS=( <%= CONFIG.sets %> )
PKG_MANAGER="<%= CONFIG.pkgmanager %>"
ARCH="<%= CONFIG.arch %>"

# Overlays Hash
<% CONFIG.overlay_array.each do |name| %>
<%= "#{name}" %>
<% end %>
